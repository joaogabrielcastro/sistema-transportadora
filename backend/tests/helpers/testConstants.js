/** Token usado nos testes de integração com AUTH_ENABLED=true (≥16 caracteres). */
export const TEST_API_TOKEN = "integration-test-token-ok";

export const minimalPdfBuffer = Buffer.from(
  "%PDF-1.4\n1 0 obj<<>>endobj\ntrailer<<>>\n%%EOF\n",
);
